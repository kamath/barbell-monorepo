{
	"name": "barbell-monorepo",
	"private": true,
	"workspaces": [
		"packages/*",
		"barbell",
		"customer-worker-1"
	],
	"scripts": {
		"build:packages": "bun run --filter '@barbell/*' build",
		"lint:fix": "biome check --write"
	}
}
